<div class="container">
    <h1>Restaurant List</h1>
    <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= link_to restaurant.name, restaurant_path(restaurant.id) %></li>
    <% end %>
    </ul>
    <p><%= link_to "Create new restaurant", new_restaurant_path, id: "link"  %></p>
</div>
